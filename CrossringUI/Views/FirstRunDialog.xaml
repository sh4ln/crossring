<Window x:Class="CrossringUI.Views.FirstRunDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to CROSSRING" 
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e" ResizeMode="NoResize">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="🛡️" FontSize="32" VerticalAlignment="Center"/>
            <StackPanel Margin="15,0,0,0">
                <TextBlock Text="CROSSRING" FontSize="28" FontWeight="Bold" Foreground="#e94560"/>
                <TextBlock Text="Zero Trust Endpoint Security" Foreground="#888" FontSize="14"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Description -->
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="#eaeaea" Margin="0,0,0,20" FontSize="14">
            Welcome! Choose your initial protection mode. This can be changed later in Settings.
        </TextBlock>
        
        <!-- Mode Selection -->
        <StackPanel Grid.Row="2">
            <RadioButton x:Name="RbMonitoring" GroupName="Mode" Margin="0,0,0,15">
                <Border Background="#252550" CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🟡" FontSize="24" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Monitoring Only (Recommended for first 7 days)" 
                                       Foreground="#fbbf24" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="Logs all activity but allows everything. Safe for testing compatibility." 
                                       Foreground="#888" TextWrapping="Wrap" FontSize="12" Margin="0,5,0,0"/>
                            <Border Background="#44403c" CornerRadius="4" Padding="8,5" Margin="0,8,0,0" HorizontalAlignment="Left">
                                <TextBlock Text="⏰ Auto-upgrades to Balanced after 7 days" Foreground="#fbbf24" FontSize="11"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </RadioButton>
            
            <RadioButton x:Name="RbBalanced" GroupName="Mode" IsChecked="True" Margin="0,0,0,15">
                <Border Background="#252550" CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🟢" FontSize="24" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Balanced (Default)" 
                                       Foreground="#4ade80" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="Blocks known threats. Prompts for unknown or unsigned software." 
                                       Foreground="#888" TextWrapping="Wrap" FontSize="12" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </RadioButton>
            
            <RadioButton x:Name="RbZeroTrust" GroupName="Mode" Margin="0,0,0,15">
                <Border Background="#252550" CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔴" FontSize="24" VerticalAlignment="Top"/>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Zero Trust (Advanced)" 
                                       Foreground="#ef4444" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="Blocks ALL unsigned or unknown software. May require frequent approvals." 
                                       Foreground="#888" TextWrapping="Wrap" FontSize="12" Margin="0,5,0,0"/>
                            <Border Background="#44403c" CornerRadius="4" Padding="8,5" Margin="0,8,0,0" HorizontalAlignment="Left">
                                <TextBlock Text="⚠️ Recommended only for security professionals" Foreground="#ef4444" FontSize="11"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </RadioButton>
        </StackPanel>
        
        <!-- Action Button -->
        <Button Grid.Row="3" Content="Start Protection ➜" Padding="30,15" HorizontalAlignment="Center"
                Click="OnStartClicked"
                Background="#e94560" Foreground="White" FontSize="16" FontWeight="SemiBold"
                BorderThickness="0" Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="8" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
